# VERITAS — Python dependencies
# Install with: pip install -r requirements.txt

# Document processing
pypdf>=3.0.0
python-docx>=0.8.11

# Image processing
Pillow>=9.0.0

# HTTP requests (AI provider API calls)
requests>=2.28.0

# Spreadsheet support
openpyxl>=3.0.0

# XML/HTML parsing
lxml>=4.9.0

# Audio/video metadata stripping (optional — app works without it)
# mutagen>=1.46.0

# AI transcription (optional — app works without it)
# faster-whisper>=0.9.0

# Google AI (optional — configure in Settings if using Gemini)
# google-generativeai>=0.3.0

# Testing
pytest>=7.0.0
